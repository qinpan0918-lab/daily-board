services:
  - type: web
    name: daily-board
    env: docker
    plan: free
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: "10000"
    disk:
      name: data
      mountPath: /app/server/data
      sizeGB: 1
